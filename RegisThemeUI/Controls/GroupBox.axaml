<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="RegisThemeUI.Controls.GroupBox"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:regis_theme="clr-namespace:RegisThemeUI.Controls"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Styles>
        <Style Selector="regis_theme|GroupBox">
            <Setter Property="Template">
                <ControlTemplate>
                    <StackPanel>
                        <TextBlock Margin="0,0,0,0" Text="{TemplateBinding Header}" Foreground="{DynamicResource RegisThemeLowText}" ></TextBlock>
                        <Border Height="1" BorderThickness="0" Margin="0,10,0,10" Background="{DynamicResource RegisThemeControlBorderBrush}"></Border>
                        <ContentControl Content="{TemplateBinding Content}" />
                    </StackPanel>
                </ControlTemplate>
            </Setter>
        </Style>
    </UserControl.Styles>
</UserControl>
